import { OrdenacaoCampoDirective } from "@app/comum/diretivas/ordenacao-campo.directive";


describe('OrdenacaoCampoDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdenacaoCampoDirective();
    expect(directive).toBeTruthy();
  });
});
