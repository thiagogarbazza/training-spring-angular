import { EnumIdentifiableWithDescription } from '@shared/models/enum-identifiable-with-description';

export class SituacaoDocumentoDispensado extends EnumIdentifiableWithDescription{

  constructor(values: Object) {
    super(values);
  }
}
